apiVersion: constraints.gatekeeper.sh/v1beta1
kind: K8sDenyRisk
metadata: { name: deny-high-risk }
spec:
  match: { kinds: [ { apiGroups: ["apps"], kinds: ["Deployment"] } ] }
  parameters: { threshold: 0.70 }
